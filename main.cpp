#include <stdio.h>
#include "Clope.h"
#include "Cluster.h"
int main(int argc, char const *argv[])
{
    int ClusterCount = 0;
    double maxCost;
    int BestChoice;
    double remCost;
    int i, j, k;
    int tr = 1;
    bool moved = false;

    int t_num = atoi(argv[1]);
    int t_len = atoi(argv[2]);
    float r = atof(argv[3]);

    printf("Hello clope");
    return 0;
}
